// let rekoList = $("movie-list");
// let rekoDone = $("[data-reko-id='<%= @reko.id %>']");

// rekoDone.remove;
// rekoList.insertAdjacentElement('beforeend', rekoDone);

function markAsDone() {
  const rekoDone = document.querySelector("[data-reko-id='<%= @reko.id %>']");
  const rekoDoneBox = rekoDone.parentNode;
  const markDoneSlide = document.querySelector('.mark-watched-slide');
  const doneSwipers = document.querySelectorAll('.light-grey-background');

  // remove delete slide from done swipers

  // get main slide to add class
  // rekoDone.querySelector('.main-slide').classList.add('light-grey-background');
  rekoDone.querySelector('.can-be-marked-done').classList.add('light-grey-background');

  rekoDoneBox.parentNode.appendChild(rekoDone);
  rekoDone.querySelector(".swiper-container").classList.add('no-swipe-left')
  rekoDone.querySelector('.mark-watched-slide').remove();
  // markDoneSlide.remove();
  // markDoneSlide.addEventListener

  // doneSwipers.forEach((node) => {
  //     node.previousElementSibling.remove();
  // });
}

markAsDone()
