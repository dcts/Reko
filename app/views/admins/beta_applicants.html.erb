<%= render partial: "navbar" %>

<div class="container">
  <h2>Beta Applicants List</h2>
  <h4>(<%= @beta_applicants.size %> Beta Applicants)</h4>
  <%= render partial: "beta_applicants/table_beta_applicants", locals: { beta_applicants: @beta_applicants } %>
</div>
