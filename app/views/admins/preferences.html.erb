<%= render partial: "navbar" %>

<div class="container">
  <h2>Preferences</h2>
  <h4>(<%= @preferences.size %> Preferences)</h4>
  <%= render partial: "preferences/table_preferences", locals: { preferences: @preferences } %>
</div>
